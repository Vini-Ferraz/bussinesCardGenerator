import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import { BussinesCard } from "./components/bussines-card-componente";
ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <BussinesCard />
  </React.StrictMode>
);
